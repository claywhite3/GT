/* =========================================================================
   Scandit configuration — TEMPLATE
   -------------------------------------------------------------------------
   1. Copy this file and rename the copy to:  config.js
   2. Paste your Scandit license key between the quotes below.
   3. Save. Do NOT commit config.js (it's listed in .gitignore).

   If config.js is missing or the key is left blank, the app automatically
   falls back to the built-in html5-qrcode scanner.
   ========================================================================= */
window.SCANDIT_LICENSE_KEY = '';
